[project]
name = "visit-counter-worker"
version = "0.1.0"
description = "GumGum visit-counter queue worker"
requires-python = ">=3.12"
dependencies = []

[project.scripts]
visit-counter-worker = "visit_counter_worker.worker:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
